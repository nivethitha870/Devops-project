apiVersion: v1
kind: Service

metadata:
  name: devops-service

spec:
  selector:
    app: devops-project

  type: NodePort

  ports:
  - protocol: TCP
    port: 5000
    targetPort: 5000
    nodePort: 30007
